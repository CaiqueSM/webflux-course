spring:
  data:
    mongodb:
      uri: mongodb+srv://${DB_USER}:${DB_PASSWORD}@cluster0.oxwqie8.mongodb.net/${DB_NAME}?retryWrites=true&w=majority
      auto-index-creation: true
  application:
    name: webflux-course